import { ListButton, ShowButton, TopToolbar } from 'react-admin';

const EditorActions = () => (
  <TopToolbar>
    <ShowButton />
    <ListButton />
  </TopToolbar>
);

export default EditorActions;
